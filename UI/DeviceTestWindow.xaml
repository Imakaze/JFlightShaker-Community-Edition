<Window x:Class="JFlightShaker.UI.DeviceTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test Device"
        Width="720"
        Height="680"
        MinWidth="560"
        MinHeight="480"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock x:Name="DeviceNameText" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock x:Name="GuidText" Foreground="Gray"/>
            <TextBlock x:Name="LastInputText" Text="Last input: None" Margin="0,5,0,0"/>
        </StackPanel>

        <Separator Grid.Row="1"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Axes" FontSize="15" FontWeight="SemiBold" Margin="0,10,0,6"/>
                <StackPanel x:Name="AxesPanel"/>

                <TextBlock Text="Buttons" FontSize="15" FontWeight="SemiBold" Margin="0,14,0,6"/>
                <WrapPanel x:Name="ButtonsPanel"/>

                <TextBlock Text="Rotaries &amp; Selectors" FontSize="15" FontWeight="SemiBold" Margin="0,14,0,6"/>
                <WrapPanel x:Name="SelectorsPanel"/>

                <TextBlock Text="POV Hats" FontSize="15" FontWeight="SemiBold" Margin="0,14,0,6"/>
                <StackPanel x:Name="PovPanel"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0">
            <TextBlock Text="Profile:" VerticalAlignment="Center" Margin="0,0,7,0"/>
            <ComboBox x:Name="ProfileCombo" Width="230"/>
        </StackPanel>
    </Grid>
</Window>
